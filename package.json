
{
  "name": "production-tracker-mvp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "seed:admin": "node --env-file=.env scripts/seed.mjs",
    "db:migrate": "echo 'This would run database migrations. See supabase/migrations/'",
    "db:reset": "echo 'This would reset the database and re-seed. Run migrate then seed.'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
